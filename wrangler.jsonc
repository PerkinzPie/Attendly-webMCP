{
  // Cloudflare Pages configuration for the static Attendly-webMCP build.
  // Deploy with `pnpm deploy` after `pnpm check` passes.
  "name": "attendly-webmcp",
  "compatibility_date": "2026-08-01",
  "pages_build_output_dir": "dist"
}
